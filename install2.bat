@echo off
REM NOTE: this used to force-install the legacy discord.js branch (v11),
REM which breaks this bot (it requires v13). That line was removed.
REM Just run install.bat (npm install) instead.
echo This bot uses discord.js v13. Run install.bat, then "node index.js".
pause
